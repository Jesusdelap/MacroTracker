package com.example.test1.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

/** Tokens semánticos de forma — usados en Theme.kt para construir MaterialTheme.shapes */
val AppShapeXs = RoundedCornerShape(4.dp)   // chips, pills, badges
val AppShapeSm = RoundedCornerShape(8.dp)   // botones pequeños, inputs
val AppShapeMd = RoundedCornerShape(12.dp)  // cards, diálogos pequeños
val AppShapeLg = RoundedCornerShape(16.dp)  // cards elevadas, bottom sheets
val AppShapeXl = RoundedCornerShape(24.dp)  // FAB, diálogos grandes
